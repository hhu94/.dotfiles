" Use terminal vim instead of gvim
set editor="urxvt -e vim"

" Show toolbar and bookmarks
set go+=TB

" Add expando buttons (RES buttons) to hint tags
set ht+=.expando-button

" Move around faster
nmap -b h 3h
nmap -b j 3j
nmap -b k 3k
nmap -b l 3l
